@model OperariosReportViewModel
@{
    Layout = null;
    var listaPeriodos = ViewBag.TodosLosPeriodos as List<StjacksAssistens.Models.Periodss>;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>Reporte Empaque - @Model.Periodo.Description</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="~/css/spreadssheet.css" />
</head>
<body class="p-4 bg-white">
    <div class="no-print p-3 mb-4 shadow-sm rounded border bg-light">
        <div class="row align-items-center">
            <div class="col-md-5 position-relative">
                <label class="fw-bold small text-muted"><i class="bi bi-search"></i> Buscar otro periodo:</label>
                <input type="text" id="periodSearchInput" class="form-control form-control-sm" placeholder="Buscar..." autocomplete="off">
                <div id="periodSearchResults" class="search-results-container" style="display: none;">
                    @foreach (var p in listaPeriodos)
                    {
                        <div class="search-item" data-id="@p.Id" data-filter="@p.Description.ToLower() @p.StartDate.ToString("dd/MM/yyyy")">
                            <strong>@p.Description</strong><br>
                            <span class="text-muted small">@p.StartDate.ToShortDateString() - @p.EndDate.ToShortDateString()</span>
                        </div>
                    }
                </div>
            </div>
            <div class="col-md-7 text-end">
                <button type="button" id="btnToggleModo" class="btn btn-primary btn-sm" onclick="toggleVista()">
                    <i class="bi bi-clock-history"></i> <span id="btnText">Por Hora</span>
                </button>
                <button onclick="window.print()" class="btn btn-dark btn-sm">
                    <i class="bi bi-printer"></i> Imprimir
                </button>
                <a href="javascript:history.back()" class="btn btn-outline-secondary btn-sm">
                    <i class="bi bi-arrow-left"></i> Regresar
                </a>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-between align-items-center mb-3">
        <span class="fw-bold" style="font-size: 14px;">INDUSTRIAS ST. JACK'S S.A. DE C.V.</span>
        <span id="txtTitulo" class="fw-bold text-decoration-underline" style="font-size: 16px;">REPORTE DE AUSENTISMO - EMPAQUE</span>
    </div>
    <table class="table-excel mb-3">
        <tr class="header-purple">
            <td style="width: 10%;">CDC</td>
            <td style="width: 40%;">Área / Departamento</td>
            <td style="width: 20%;">Tipo de Planilla</td>
            <td style="width: 30%;">Período de Pago</td>
        </tr>
        <tr>
            <td>@Model.CDC</td>
            <td>@Model.AreaNombre</td>
            <td>@Model.TipoPlanilla</td>
            <td class="fw-bold">Del @Model.Periodo.StartDate.ToString("dd/MM/yyyy") al @Model.Periodo.EndDate.ToString("dd/MM/yyyy")</td>
        </tr>
    </table>
    <table class="table-excel">
        <thead>
            <tr class="header-purple">
                <th rowspan="2" style="width: 30px;">No.</th>
                <th rowspan="2" style="width: 60px;">Código</th>
                <th rowspan="2">Nombre del Empleado</th>
                <th colspan="5">Días de la Semana</th>
                <th rowspan="2" class="modo-hora-col col-tiempo-input" style="width: 110px;">Tiempo (H:M)</th>
                <th rowspan="2" class="modo-hora-col" style="width: 90px;">Descuento</th>
                <th rowspan="2">Motivo / Observaciones</th>
            </tr>
            <tr class="header-purple">
                <th>Lun</th>
                <th>Mar</th>
                <th>Mie</th>
                <th>Jue</th>
                <th>Vie</th>
            </tr>
        </thead>
        <tbody>
            @{
                int count = 1;
            }
            @foreach (var emp in Model.Empleados)
            {
                var sem1 = emp.Semanas.ElementAtOrDefault(0);
                var sem2 = emp.Semanas.ElementAtOrDefault(1);
                <tr>
                    <td rowspan="2">@count</td>
                    <td rowspan="2" class="fw-bold">@emp.Codigo</td>
                    <td rowspan="2" class="text-start ps-2">@emp.Nombre</td>
                    <td>@(sem1?.Lunes?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem1?.Martes?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem1?.Miercoles?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem1?.Jueves?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem1?.Viernes?.ToString("dd-MMM") ?? "")</td>
                    <td rowspan="2" class="modo-hora-col col-tiempo-input">
                        <div class="time-input-group">
                            <input type="number" class="input-time in-h" placeholder="0" oninput="calcular(this)">
                            <b>:</b>
                            <input type="number" class="input-time in-m" placeholder="0" oninput="calcular(this)">
                        </div>
                    </td>
                    <td rowspan="2" class="modo-hora-col">
                        <span class="label-descuento">$ 0.00</span>
                    </td>
                    <td class="text-start small italic p-1">
                        <div class="editable-motivo" contenteditable="true" data-opid="@emp.Codigo" data-period="@Model.Periodo.Id" onblur="saveObservation(this)">
                            @(sem1?.Motivo ?? "")
                        </div>
                    </td>
                </tr>
                <tr>
                    <td>@(sem2?.Lunes?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem2?.Martes?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem2?.Miercoles?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem2?.Jueves?.ToString("dd-MMM") ?? "")</td>
                    <td>@(sem2?.Viernes?.ToString("dd-MMM") ?? "")</td>
                    <td class="text-start small italic p-1">
                        <div class="editable-motivo" contenteditable="true" data-opid="@emp.Codigo" data-period="@Model.Periodo.Id" onblur="saveObservation(this)">
                            @(sem2?.Motivo ?? "")
                        </div>
                    </td>
                </tr>
                count++;
            }
        </tbody>
    </table>

    <div class="container-fluid text-center mt-5 signature-section">
        <div class="row">
            <div class="col-4">
                <div style="border-top: 1px solid black; width: 80%; margin: 0 auto;"></div>
                <div class="fw-bold small">Elaborado por: Willian Alexander</div>
                <div class="small">Digitador</div>
            </div>
            <div class="col-4">
                <div style="border-top: 1px solid black; width: 80%; margin: 0 auto;"></div>
                <div class="fw-bold small">Revisado por: Luis Barahona</div>
                <div class="small">Jefe de Empaque</div>
            </div>
            <div class="col-4">
                <div style="border-top: 1px solid black; width: 80%; margin: 0 auto;"></div>
                <div class="fw-bold small">Autorizado por: Johanna Quiñónez</div>
                <div class="small">Gerente de produccion</div>
            </div>
        </div>
    </div>
    <script>
        function toggleVista() {
            const horasCol = document.querySelectorAll('.modo-hora-col');
            const btnText = document.getElementById('btnText');
            const titulo = document.getElementById('txtTitulo');

            if (horasCol[0].style.display === 'none' || horasCol[0].style.display === '') {
                horasCol.forEach(el => el.style.display = 'table-cell');
                btnText.innerText = "Quitar Modo Hora";
                titulo.innerText = "REPORTE DE DESCUENTOS POR HORA - EMPAQUE";
            } else {
                horasCol.forEach(el => el.style.display = 'none');
                btnText.innerText = "Por Hora";
                titulo.innerText = "REPORTE DE AUSENTISMO - EMPAQUE";
            }
        }
        function calcular(el) {
            const row = el.closest('tr');
            const h = parseFloat(row.querySelector('.in-h').value) || 0;
            const m = parseFloat(row.querySelector('.in-m').value) || 0;
            const res = row.querySelector('.label-descuento');

            const minutosTotales = (h * 60) + m;
            const monto = minutosTotales * 0.0275;
            res.innerText = "$ " + monto.toFixed(2);
        }
        const input = document.getElementById('periodSearchInput');
        const container = document.getElementById('periodSearchResults');
        input.addEventListener('input', function() {
            const searchText = this.value.toLowerCase().trim();
            if (searchText.length === 0) { container.style.display = 'none'; return; }
            let found = 0;
            document.querySelectorAll('.search-item').forEach(item => {
                if (item.getAttribute('data-filter').includes(searchText)) {
                    item.style.display = 'block'; found++;
                } else item.style.display = 'none';
            });
            container.style.display = found > 0 ? 'block' : 'none';
        });
        document.querySelectorAll('.search-item').forEach(item => {
            item.addEventListener('click', function() {
                window.location.href = '/Ausentismo/ReporteEmpaque?periodId=' + this.getAttribute('data-id');
            });
        });
        function saveObservation(element) {
            const text = element.innerText.trim();
            element.classList.add('saving');
            fetch('/Ausentismo/GuardarObservacion', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    OperatorCode: element.getAttribute('data-opid'),
                    PeriodId: parseInt(element.getAttribute('data-period')),
                    Observation: text
                })
            }).then(r => {
                element.classList.remove('saving');
                if(!r.ok) alert("Error al guardar.");
            }).catch(() => element.classList.remove('saving'));
        }
    </script>
</body>
</html>